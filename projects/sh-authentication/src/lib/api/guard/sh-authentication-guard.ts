import { Injectable } from '@angular/core';import {CanActivate, Router} from '@angular/router';import {SHAuthenticationBaseService} from '../services/sh-authentication-base.service';@Injectable({    providedIn: 'root',})export class AuthGuard implements CanActivate {    constructor(        public authenticationService: SHAuthenticationBaseService,        public router: Router,    ) {}    canActivate():boolean {        return this.isUserLoggedIn();    }    private isUserLoggedIn():boolean {        if (this.authenticationService.isAuthenticate()) return true;        this.router.navigate(['/login']);        throw new Error('vous devez vous connectez pour avoir accés au système')    }}